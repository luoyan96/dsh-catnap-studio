# Catnap Studio is a self-contained aggregate. Its host and browser entries
# activate the vendored Apache-2.0 feature modules from one Cordis row.
- insert:
    - id: ui-skin-catnap
      name: 'dsh-catnap-plugins'
